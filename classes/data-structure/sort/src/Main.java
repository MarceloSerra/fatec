import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        Integer[] sortData = {69, 68, 32, 36, 91, 19, 28, 44, 88, 73, 37, 99, 1};

        // Integer[] bubbleSortResult =  SortStrategy.bubble(sortData);
        // System.out.println(Arrays.toString(bubbleSortResult));

        // Integer[] mergeSortResult =  SortStrategy.merge(sortData, sortData.length);
        // System.out.println(Arrays.toString(mergeSortResult));
    }
}